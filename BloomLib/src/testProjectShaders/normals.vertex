attribute vec4 a_position; 
attribute vec3 a_normal;
attribute vec2 a_texCoord0; 
uniform mat4 u_projectionViewMatrix;
varying vec3 v_normals;
void main()
{
	v_normals = vec3(a_normal);
	gl_Position = u_projectionViewMatrix * a_position;
}